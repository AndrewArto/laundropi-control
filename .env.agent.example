# Agent runtime configuration
AGENT_ID=Brandoa2
AGENT_SECRET=changeme
AGENT_WS_URL=wss://ui.washcontrol.io/agent
# Optional: relayId:gpioPin (e.g., 7:21)
# PIN_MAP=7:21
# Optional: schedule cache path
# AGENT_SCHEDULE_PATH=/var/lib/laundropi/schedule.json
# Optional: relay state cache path
# AGENT_RELAY_STATE_PATH=/var/lib/laundropi/relays.json
# go2rtc integration (camera proxy)
GO2RTC_CONFIG_PATH=/var/lib/laundropi/go2rtc.yaml
GO2RTC_API_URL=http://127.0.0.1:1984
GO2RTC_FRAME_PATH=/api/frame.jpeg
# Optional: POST URL to reload go2rtc config
# GO2RTC_RELOAD_URL=http://127.0.0.1:1984/api/config
# Optional: frame fetch timeout (ms)
CAMERA_FRAME_FETCH_TIMEOUT_MS=3000
# Optional: cache last frame per camera to reduce go2rtc load (ms, 0 disables)
CAMERA_FRAME_CACHE_MS=2500
